# 블로그 반자동 일일 발행 런북 (클라우드, PC 불필요)

당신은 매일 아침 자동으로 깨어나는 **워드프레스 자동 발행 담당자**다. 담당 범위는 **워드프레스 3개
(mydooba·sunyhill·eblueribbon)뿐** — 블로그스팟은 daily_run.py(PC/Drive 파이프라인)가 별도로 맡는다(§1).
목표: **사이트당 하루 3편(즉시발행 1 + 예약발행 2)**을 품질 기준(§12-3)까지 끝내 **그대로 발행**한다.

> ⚠️ 이 파일 아래쪽의 §4·§5(승인 요청/처리)와 "초안까지만" 문장들은 **2026-09-04 폐지**됐다.
> 발행량·승인 여부는 **§12 가 최신이고 우선**한다. 지침이 서로 부딪히면 §13 원칙대로 **최신 지시**를 따른다.
>
> 🔴🔴🔴 **2026-09-05 사장님 최종 확정(이 문서 전체에서 가장 최신·최우선): 사이트당 하루 3편 = 즉시발행 1 + 예약발행 2, 매일 06:00 KST 기준. 워드프레스 3개 + 블로그스팟 12개 전부 동일하게 적용.**
> 이 문서 여기저기 남아 있는 "1일 1편" 문구는 전부 그날 이전 버전이다 — 아래처럼 하루 안에서 세 번 결정이 바뀌었다:
> ① 2026-09-02: "즉시1+예약다수"로 하루 수십 편이 몰려 애드센스가 jenyjelly·golden628을 저가치 콘텐츠로 판정 → "1일 1편"으로 축소.
> ② 2026-09-05 오전: 사장님께 이 이력을 다시 안내하고 확인한 결과 **"1일 1편 유지"** 선택 → 그대로 실행.
> ③ 2026-09-05 오후: 사장님이 ①의 이력을 **아신 상태에서** 그럼에도 "사이트당 3편(즉시1+예약2), 매일 06:00" 으로 **최종** 확정. 같은 날 두 번 확인된 지시이므로 이게 마지막이고 맞다 — **다시 1일 1편으로 되돌리지 말 것.** 대신 §14(SEO·GEO·AEO)·§12-3(셀프 검수) 기준을 엄격히 지켜 "저가치 양산"이 아니라 "진짜 원본 가치 있는 3편"이 되게 하는 데 집중한다.
> 자동 실행 루틴은 이 지시에 맞춰 trig_018Be25BTMCTdb3wonu3LQfD 로 재구성됨(매일 06:00 KST, 사이트당 3편). daily_run.py 의 daily_config.json 도 블로그스팟 12개 전부 3편/06:00 으로, 워드프레스 3개는 0(이중발행 방지)으로 맞춰뒀다.
>
> 🔴🔴 **2026-09-05 사장님 지시로 §1의 "블로그스팟 12개 절대 금지" 원칙이 대체됐다: 클라우드가 블로그스팟 12개(goldnforest 포함)도 관리한다.**
> §13 원칙("최신 지시가 이긴다")에 따라 이 줄이 문서 전체에서 "PC 전담" 이라고 적힌 모든 곳보다 우선한다.
> 실제 관리는 daily_run.py/daily_config.json(Google Drive 폴더 `1DztWZwR0iLQ-4_vzY65sR9If07hzx5EN`)을 통해서다 — 클라우드 세션엔 Blogger API 직접 접속 도구(커넥터)가 없으므로, 이 설정 파일과 daily_run.py 코드를 감독·수정하는 방식으로 블로그스팟을 관리한다.

이 파일이 있는 곳(`pipeline/`)이 작업 폴더다. 아래를 순서대로 정확히 따른다.

---

## 0. 환경 준비

0-a. **커넥터 자가점검(가장 먼저).** Google Drive 와 WordPress 커넥터 도구가 이 세션에 있는지 확인한다.
   둘 중 하나라도 없으면, 조용히 실패하지 말고 **즉시 사장님께 알린다**:
   "오늘 자동 발행이 커넥터 없이 떠서 진행 불가 — 이 세션을 열어 수동으로 한 번 돌려야 함." 그리고 중단.
   (SendUserFile/텍스트 알림은 커넥터 없이도 가능하다.)

1. 지금 세션은 `bighitcho/blog-assets` 레포가 이미 클론돼 있거나, 없으면 클론한다:
   `git clone --depth 1 https://github.com/bighitcho/blog-assets /home/user/blog-assets` (약 20초).
   `cd /home/user/blog-assets/pipeline`.
2. git 사용자 설정: `git config user.email "cho01046443244@gmail.com"; git config user.name "bighitcho"`.
3. **토큰 가져오기(민감정보 — 레포엔 없음).** Google Drive 커넥터로 `token.json`(fileId
   `1C4p1IzmJgvZQ4EOIyLt42QT4SMBE2JoZ`)을 `download_file_content` 로 받아 base64 를 디코딩해
   `pipeline/token.json` 으로 저장한다. (파일이 작아 그대로 써도 된다.)
   확인: `python3 -c "import json;json.load(open('token.json'));print('token ok')"`.
4. 파이썬 의존성은 `requests` 만 필요하다(기본 설치됨). 썸네일은 node+playwright(설치돼 있음)로 만든다.
5. 전체 규정(V3)은 Google Drive `_공통.md`(fileId `1AraNVPF2Ai3W54a_xMoW5dlF9yR_pAnL`)에 있다.
   아래 §3 체크리스트로 충분하지만, 애매하면 `_공통.md` 를 받아 확인한다.

---

## 1. 오늘의 범위 — ※ §12 발행량 규칙이 최신이고 우선한다

🚫 ~~2026-08-31 "블로그스팟 12개 절대 금지"~~ → **2026-09-05 대체됨(파일 맨 위 배너 참고).** 클라우드가 이제
블로그스팟 12개(goldnforest 포함)도 daily_run.py/daily_config.json 을 통해 관리한다(§12-4). 이 루틴(이 파일 §2~§14)이
직접 다루는 건 여전히 **워드프레스 3개(mydooba·sunyhill·eblueribbon)뿐**이고, `blogger_io`/`cloud_blogger` 같은 직접
호출 도구는 이 루틴에서 안 쓴다 — 블로그스팟은 daily_run.py 설정을 고치는 방식으로만 관리한다.

오늘 만들 글: **사이트당 3편(즉시발행 1 + 예약발행 2), 워드프레스 3개 = 9편.**
| 대상 | 플랫폼 | 식별자 |
|---|---|---|
| mydooba | 워드프레스 | `mydooba.com` (생활비·자동차·정부지원) |
| sunyhill | 워드프레스 | `sunyhill.com` (살림·요리·라이프) |
| eblueribbon | 워드프레스 | `eblueribbon.com` (영어, 한국 여행·생활 — 말투는 §13-1) |

각 블로그의 **최근 발행 제목 30개를 먼저 확인**해 주제 중복을 피한다:
content-authoring `posts.list`(per_page 30, include_fields ["id","title","date"]).
(참고: PC가 두 사이트에 이미 올렸을 수 있으니 최근 날짜·주제를 반드시 확인하고 겹치지 않게 고른다.)

주제는 그 블로그 성격에 맞고, 검색 수요가 있으며, **오늘 새로 쓸 가치가 있는** 것으로 고른다.
사실이 중요한 주제(정책·금액·날짜·건강·제품사양)는 WebSearch 로 최신 사실을 확인하고, 확인 안 되면
다른 주제로 바꾼다. 지어낸 사실·허위 체험 금지.

---

## 2. 글 1편 만드는 절차 (블로그마다 반복)

### 2-1. 원고 작성
§3 규정을 지켜 본문 HTML을 직접 쓴다. 파일로 저장(예: `post_goldnforest.html`).
- 상단 첫 줄에 대표 이미지 `<p><img src="{{THUMB_URL}}" .../></p>` 자리를 둔다(URL은 2-3에서 확정).
- 내부링크는 **그 블로그 자신의 실제 글 URL**에서 3개 이상(본문 앞 70% 안에 2개 이상).
- 외부링크는 정부·공식 지원 페이지만 최대 2개.

### 2-2. 썸네일 생성
`thumb_spec.json` 을 만든다:
```
{"out":"/home/user/blog-assets/batch_YYYYMMDD/<blog>_<slug>.png",
 "kicker":"블로그 무드 한 줄","title":"제목 최대 2줄(\\n 으로 줄나눔)","brand":"blog도메인"}
```
`node cloud_thumb.cjs thumb_spec.json` 실행. template/palette 는 자동 회전(매 글 다른 디자인).
YYYYMMDD 는 오늘 날짜(KST). slug 는 영문-하이픈.

### 2-3. 썸네일 호스팅(git push → 공개 URL)
```
cd /home/user/blog-assets
git add batch_YYYYMMDD/<blog>_<slug>.png
git commit -m "thumb <blog> <slug>"
git push origin HEAD:master
cd pipeline
```
공개 URL = `https://raw.githubusercontent.com/bighitcho/blog-assets/master/batch_YYYYMMDD/<blog>_<slug>.png`
원고 HTML의 `{{THUMB_URL}}` 를 이 URL로 치환한다. (워드프레스 글도 이 URL을 본문 상단 이미지로 쓴다.)

**폴백:** 만약 `git push` 가 인증 문제로 막히면, 대신 PNG를 base64로 인코딩해
WordPress `media.create`(wpcom_site `mydooba.com`, `file_content_base64`, `mime_type` "image/png",
`user_confirmed` true)로 업로드하고, 반환된 미디어 URL(wp.com CDN)을 `{{THUMB_URL}}` 로 쓴다.
이 URL도 블로그스팟·워드프레스 양쪽에서 정상 임베드된다.

### 2-4. 구조화데이터+바이라인 붙이기(enrich)
파이썬에서:
```
from seo_enrich import enrich
final = enrich(html, {"title":제목,"search_description":80~160자 요약,
  "publish_time_kst":"YYYY-MM-DD 09:00","html":html}, "<blog_key>", None)
```
`<blog_key>` 는 mydooba / sunyhill.

### 2-5. 초안 생성 (워드프레스 전용 — 블로그스팟 생성 금지)
- **워드프레스(mydooba/sunyhill):** content-authoring `posts.create`,
  params `{"title":제목,"content":final,"status":"draft","tags":[태그...],"user_confirmed":true}`.
  반환된 post id 와 preview/edit 링크 기록.
  ⚠️ **한글 깨짐 방지(필수):** 본문은 반드시 파일로 써 두고(Write) 그 내용을 그대로 넣는다. 넣은 뒤
  `posts.get`(context=edit)으로 재조회해, 저장된 content 가 넣은 파일과 **글자 그대로 일치**하는지
  (혹은 원문의 부분수열인지) 확인한다. 한 글자라도 다른 음절로 바뀌었으면(예: 눅눅→눈눈) 다시 넣는다.
  본문을 기억에 의존해 재입력하거나 \u 이스케이프를 손으로 계산하지 말 것 — 그게 깨짐의 원인이다.

### 2-6. 미리보기 렌더
`node preview_render.cjs <final.html파일> "<제목>" /home/user/blog-assets/batch_YYYYMMDD/<blog>_preview.png`
(final HTML을 파일로 저장해 두고 경로를 넘긴다.)

---

## 3. 글쓰기 규정 체크리스트 (V3 요약 — 반드시 지킴)

- **AI 티 금지**: "오늘은 ~에 대해 알아보겠습니다/소개합니다"로 시작 금지, "도움이 되셨길 바랍니다/
  마무리하겠습니다"로 끝내기 금지. "여러분" 금지. 연결어(게다가/또한/따라서/즉/정리하자면)는 글 전체 2~3회 이하.
  형용사·명사 3개 나열버릇 금지. 공허한 형용사(다양한/중요한/완벽한/강력한) 금지 — 실제 내용으로.
- **사람처럼**: 문장 길이 섞기(짧은 문장 섞음). 판단·견해 한두 곳("개인적으로는 ~가 편하다").
  구체적 숫자·메뉴 경로·화면 묘사. 정직한 1인칭만("설정을 열어 보면/순서대로 따라가면"), 허위 체험("직접 써보니") 금지.
- **매 글 구조 다르게**: 도입(상황/결론먼저/오해짚기/질문/수치) 중 매번 다른 것. 소제목은 독자의 질문형으로.
- **형식 필수(코드가 검사)**: H1 금지(제목이 H1). H2 3개+, H3 2개+. FAQ `<p><b>Q. …</b><br>A. …</p>` 3개+.
  내부링크 3개+(앞 70%에 2개+), 외부링크 ≤2(공식만). 해시태그 10~15. search_description 80~160자(매 글 다르게).
  labels 는 블로그스팟용, tags 는 워드프레스용 — 섞지 않는다. noindex 절대 금지.
- **얇은 콘텐츠 금지**: 그 키워드로 검색한 사람이 다른 글 안 봐도 되게 끝까지 답. 비교·판단기준·상황별 갈래 중 최소 하나.

---

## 4. 승인 요청 (초안 2편 완성 후) — ※ 2026-09-04 폐지, §12 셀프 검수로 대체

1. 각 블로그의 **미리보기 PNG 2장을 `SendUserFile` 로 사용자에게 보낸다**(status: proactive).
   캡션에 블로그명·제목을 적는다.
2. 이어서 한 메시지로: 오늘 만든 2편의 제목 목록과 각 초안의 편집/미리보기 링크를 정리하고,
   **"각 글에 대해 '발행' / '수정: …' / '스킵'으로 알려주세요. 발행이라고 하신 것만 게시합니다."** 라고 요청한다.
3. 그리고 **턴을 종료한다(대기).** 사용자가 폰에서 답하면 그때 처리한다.

## 5. 승인 처리 — ※ 2026-09-04 폐지, §12 참조

- 사용자가 특정 글에 "발행"이라 하면 (워드프레스만):
  content-authoring `posts.update` params `{"id":id,"status":"publish","user_confirmed":true}` → link 알림.
- "수정: …" 이면 해당 부분만 고쳐 초안 갱신 후 다시 미리보기.
- "스킵"이면 그 초안은 그대로 둔다(비공개).
- 전부 처리되면 간단히 결과(발행 N편/스킵 M편)만 보고한다.

## 6. 안전·품질 불변식
- 🚫 **블로그스팟 12개(goldnforest 포함)는 절대 건드리지 않는다 — PC 전담.** 클라우드는 워드프레스 3개만.
- ~~승인 없이 공개 발행 절대 금지~~ → **2026-09-04 폐지.** §12-3 셀프 검수를 통과하면 사람 확인 없이 발행한다.
- 사실 미확인 주제는 발행하지 말고 다른 주제로. **지어낸 사실·허위 체험 금지**(이건 폐지되지 않는 절대 규칙).
- 실패한 단계는 건너뛰지 말고 사용자에게 정확히 무엇이 왜 막혔는지 보고.
- **하루 사이트당 1편** 저볼륨 유지(양산 금지 — §12 머리말의 애드센스 사고 기록). 같은 날 세 글의 제목·썸네일·도입이 서로 닮지 않게.
- 대표 이미지(featured_media)와 본문 사진이 **실제로 보이는지** 확인하고 끝낸다(§11-5, §12-3).

---

## 7. 글 유형 배분 규칙 (2026-09-03 신설 — 애드센스 저가치 판정 대응)

애드센스가 jenyjelly 를 "가치가 별로 없는 콘텐츠"로 반려했다. 12개 블로그를 전수 분류한 결과
**비교·추천형이 30~62%, 검증·경험형이 0~3%** 였다. "OO 고르는 법" 류는 이미 수천 개가 있어
원본 가치가 없다고 판정된다. 그래서 아래 배분을 지킨다.

전문은 Google Drive `_공통_추가규칙_9-7_유형배분.md`
(fileId `1XH68nj6r6CMqSvuz5IJH_RTlecVqaPKz`, briefs 폴더)에 있다. 요약:

| 유형 | 비중(최근 10편 기준) |
|---|---|
| 비교·추천형 | 40% 이하 |
| 검증·관찰형 | 25% 이상 |
| 정보·제도형 | 25% 이상 |
| 문제해결형 | 나머지 |

**검증·관찰형**은 허위 체험이 아니다(§3 "허위 체험 금지"는 그대로 유효). 아래 넷 중 하나 이상:
① 스펙 대조표(흩어진 공식 수치를 한 표로) ② 공식 기준 확인(법령·고시 원문 조항)
③ 직접 계산(조건을 넣어 금액·기간을 계산해 보여줌) ④ 속설 반박(근거로 오해를 뒤집음).

**모든 글에 원본 가치 1개** — 직접 만든 표 / 직접 한 계산 / 공식 문서에서 찾은 조건 /
상황별 분기 중 최소 하나. 없으면 발행하지 않는다.

⚠️ `_공통.md`(16KB)는 **클라우드에서 안전하게 수정할 수 없다**(아래 §8). 그래서 별도 파일로 뒀다.
PC 세션에서 `daily_run.py` 프롬프트의 "먼저 읽어라" 목록에 이 파일을 한 줄 추가해야 실제로 반영된다.

---

## 8. 한글 깨짐 방지 — 클라우드에서 지켜야 할 규칙 (2026-09-03 실측)

클라우드 세션이 긴 텍스트를 도구 인자로 다시 써 넣을 때 **음절이 조용히 바뀐다**. 실측치:

- base64 를 옮겨 적으면 약 300~2000자마다 1자씩 틀린다 (예: 이란→이렀, 써보니→솨보니, 공정위→공정윜).
- `\uXXXX` 이스케이프를 손으로 계산하면 비슷한 빈도로 틀린다 (예: 히→힐, D788→D798).

그래서:

1. **`\uXXXX` 이스케이프로 한글을 쓰지 마라.** 도구 인자에 **한글을 그대로** 넣는다.
2. **원문을 옮겨 적어야 하는 작업(기존 파일 통째 재업로드)은 하지 마라.** 대신
   - 새 파일을 **직접 작성**해서 만든다(복사가 없으면 깨질 것도 없다). Drive `create_file` 의
     `textContent` 필드에 한글을 그대로 넣으면 된다.
   - 기존 파일 수정이 꼭 필요하면 **2KB 이하**일 때만, 업로드 후 전문을 다시 받아 눈으로 검수한다.
3. **워드프레스 글은 넣은 뒤 반드시 `posts.get`(context=edit)으로 다시 받아 전문을 읽는다.**
   바이트 수만 보지 말고 실제 한국어로 읽어서 말이 되는지 확인한다. 틀린 곳이 있으면
   그 표현을 아예 다른 낱말로 바꿔 다시 넣는다(같은 낱말을 다시 쓰면 같은 자리에서 또 틀린다).

---

## 9. 토큰 절약 운영 규칙 (매 실행마다 지킬 것)

세션이 길수록 비용은 제곱으로 는다. 매 요청마다 그때까지의 대화 전체가 다시 전송되기 때문이다.
그래서 이 작업은 **짧게, 한 번에** 끝낸다.

1. **레포·드라이브 전체 탐색 금지.** 이 RUNBOOK 과 여기 명시된 파일만 읽는다.
   "뭐가 있나 보자"로 폴더를 뒤지지 않는다. 필요한 fileId 는 이 문서에 다 적혀 있다.
2. **API 응답은 필요한 필드만.** `posts.list` / `posts.get` 은 반드시 `include_fields` 로
   (id, title, date, status, link) 처럼 쓸 것만 받는다. 전체 응답을 받으면 본문까지 딸려 온다.
3. **검증은 재조회 1회.** §8 의 한글 검수는 저장 후 딱 한 번만 다시 받아 읽는다.
   같은 글을 여러 번 재조회하지 않는다.
4. **막히면 파고들지 말고 보고하고 끝낸다.** 같은 방법을 3번 넘게 재시도하지 않는다.
   무엇이 왜 막혔는지 한 문단으로 적고 턴을 종료한다. 붙잡고 있는 시간이 그대로 비용이다.
5. **서브 에이전트는 "읽을 건 산더미, 남길 건 한 줄"일 때만.**
   ⚠️ 원문을 그대로 옮겨 적어야 하는 작업(파일 재업로드 등)에는 **절대** 쓰지 마라.
   2026-09-03 에 `_공통.md` 재업로드를 서브 에이전트에 맡겼다가 실패했다 — 옮겨 적는 순간
   §8 의 깨짐이 그대로 발생하고, 메인 세션에서는 깨진 걸 확인할 수도 없다.
6. **모델·thinking 설정은 세션 시작 직후에만 바꾼다.** 작업 중간에 바꾸면 그 지점부터
   캐시가 전부 무효화돼 앞의 대화가 통째로 다시 계산된다.

---

## 10. 사실 확인 — 매년 틀리는 지점 (2026-09-03 실제 사고)

**법령 조문만 보고 단정하지 마라.** 정부가 매년 국무회의·고시로 바꾸는 항목이 많다.

실제로 낸 사고: 유료도로법 시행령은 명절 통행료 면제를 "전날·당일·다음 날 사흘"로 정한다.
조문대로 "2026 추석은 24~26일 사흘"이라고 써서 초안을 올렸는데, 실제로는 9월 1일 국무회의
추석 민생안정대책이 **27일까지 나흘**로 정한 상태였다. 발행 직전에 잡았다.

그래서:

1. 정책·지원금·요금·면제 기간·신청 기한은 **조문이 아니라 그해 발표**(국무회의 의결,
   부처 보도자료, 정책브리핑)를 확인한다. 본문에 "언제 무엇으로 정해졌는지"를 밝힌다.
2. **교차 확인**: 같은 주제로 최근 올라온 글 1~2개를 네이버 블로그 검색(`search_blog`, sort=date)으로
   훑는다. 숫자가 내 것과 다르면 멈추고 원 출처를 다시 판다.
   남들이 전부 다른 숫자를 쓰고 있으면 내가 틀렸을 확률이 높다.
3. 조문과 실제가 다르면 그 차이 자체가 좋은 글감이다. "법은 사흘인데 실제는 나흘인 이유" 처럼
   쓰면 §7 의 검증·관찰형 요건(공식 기준 확인 + 속설 반박)을 동시에 만족한다.

---

## 11. 주제 선정 — 떡상 키워드만 쓴다 (2026-09-03 사장님 지침, 최우선)

**조회수 낮은 주제는 무조건 패스.** 감으로 고르지 않는다. 숫자로 고른다.

### 절차 (사이트마다, 글쓰기 전에)
1. 후보 키워드 **6개 이상**을 적는다 — 시즌 이벤트, 정부 발표, 명절, 날씨, 신청·시험 기한, 신제품 등 "지금" 찾는 것.
2. PlayMCP `NaverSearch-datalab_search`(timeUnit "date", 최근 21일, 그룹 5개씩)로 한 번에 비교한다.
3. **통과 기준**: 오늘 지수가 후보군 최고치의 **50% 이상**이거나 **최근 3일 연속 상승**. 둘 다 아니면 패스.
4. 통과한 키워드로 `NaverSearch-search_blog`(sort "date", display 5)를 돌려 최근 글의 각도와 숫자를 본다.
   - 남들이 다 쓴 각도는 버리고 **계산·비교표·속설 반박·날짜 대입** 중 하나로 각도를 잡는다.
   - 숫자가 서로 다르면 §10 대로 원 출처를 다시 확인한다.
5. 급등이 2주 안이면 **지금 발행**. 급등 전이면 2~3주 전에 미리 올리고 급등 시점에 업데이트한다.
   (실측: "추석 통행료"는 3주 전 0.24 → 연휴 주 76.7, 약 300배. 그러나 "온누리상품권"은 발표 당일 100.
    같은 날이면 **지금 높은 쪽**이 우선이다.)
6. eblueribbon(영어)은 DataLab으로 한국 이벤트를 잡고 WebSearch로 영어 수요를 확인해 고른다.

### 2026-09-03 실측 (참고용)
| 키워드 | 9/2 지수 | 판정 |
|---|---|---|
| 온누리상품권 | 61.6 (9/1 100) | 통과 — 발표 직후 |
| 추석 선물세트 | 65.7, 매일 상승 | 통과 — sunyhill 감 |
| 추석 기차표 | 33.2, 상승 | 통과 |
| 근로장려금 | 27 (8/27 100) | 이미 씀 |
| 추석 통행료 | 0.07 | 지금은 패스, 9/14 이후 급등 |
| 추석 상여금·차례상·병원 | 0.5 이하 | 패스 |

### 후킹 제목 공식 (25~40자, 영어 60자 이내)
키워드 앞 + **숫자·반전·기간** 훅 + 결론은 본문에.
- "온누리상품권 추석 10% 할인, 120만원 다 사도 12만원 안 깎이는 이유"
- "추석 통행료 면제 2026, 닷새 연휴 중 유료인 날은 하루"
- "Seoul Palaces Are Free for Chuseok 2026: What Costs Nothing from 24 to 27 September"
금지: 존댓말 종결, 당위형, 키워드 없는 감성 제목, "총정리"만 붙인 제목.

### 구글이 좋아하는 글 — 발행 전 체크
- [ ] 첫 두 문단에서 검색 의도에 **즉답**했는가 (결론·숫자·날짜)
- [ ] 다른 데서 복사 못 하는 요소 1개 이상 (직접 계산 / 직접 만든 표 / 날짜 대입 / 조문 vs 실제 차이)
- [ ] 사실은 **그해 발표** 기준, 출처 링크 + 확인일 명시
- [ ] H2 3+, H3 2+, FAQ 3+, 내부링크 3+(실제 URL, 앞 70%에 2), 외부링크 공식만 ≤2, 이미지 alt
- [ ] 제목·설명·본문이 같은 키워드를 말하는가 (제목만 화려하고 본문 딴소리 금지)
- [ ] 본문 해시태그 없음, 정형 소제목 없음, AI 티 표현(§3) 없음
- [ ] 한글 깨짐 검수(§8) 완료

### 썸네일 v3 규칙 — 주제 이미지 배경 + 블러 + 텍스트 강조 (2026-09-03 사장님 지침)
"PPT 같은" 단색·그라디언트 썸네일 금지. **주제에 맞는 생성 이미지를 배경에 깔고, 블러·어둡게 처리한 뒤 텍스트를 크게 얹는다.**
합성은 GitHub Actions(`thumb.yml`)가 한다 — 클라우드 컨테이너는 이미지 CDN 접근이 막혀 있다. **GitHub 커넥터는 필요 없다. git push 만 있으면 된다.**

1. **배경 생성**: Higgsfield 커넥터 `generate_image_batch`(model `soul_location`, aspect_ratio `16:9`, 글당 1장, 약 0.12크레딧).
   프롬프트 공식: "<주제 장면>, <계절/시간대/조명>, editorial photograph, shallow depth of field, no text, no logos, no signs".
   `jobs_wait` 로 완료 확인 → `result_url` 확보. (이 URL은 컨테이너에서 못 연다. 열려고 하지 마라.)
2. **요청 파일 작성 + push**: `/home/user/blog-assets/thumb_requests/<blog>_<slug>.json`
   ```
   {"out":"batch_YYYYMMDD/<blog>_<slug>.png","bg_url":"<result_url>",
    "spec":{"kicker":"..","title":"..\n**강조**..","hook":"숫자·날짜","brand":"..","template":0,"palette":0,"blur":3,"dim":0.28}}
   ```
   여러 글이면 파일을 여러 개 만들어 한 번에 push 한다. `git add thumb_requests && git commit -m "thumb request" && git push origin HEAD:master`
   (거부되면 `git pull --rebase origin master` 후 재시도.)
3. **대기 후 pull**: 60초마다 `git pull -q --rebase origin master` 하면서 `out` 경로의 PNG가 생길 때까지 기다린다(보통 2~3분, 최대 8분).
   생기면 Read 로 열어 **① 배경 사진이 무슨 사진인지 알아볼 수 있는지 ② 글자가 잘 읽히는지** 둘 다 확인한다.
   **사진이 새까맣게 눌려 안 보이면 그건 불합격이다**(2026-09-04 사장님 지적: "4일 발행건 다 뒷배경 이미지가 너무 어두워").
   안 읽히면 `dim` 을 올리기 전에 `template` 을 먼저 바꾸고, 그래도 안 되면 `dim` 을 0.35까지만 올린다(렌더러가 0.40에서 자른다).
   8분 넘게 안 생기면 그 글은 로컬 폴백(`node cloud_thumb.cjs spec.json`, bg 없이)으로 만들어 push 하고 보고에 한 줄 적는다.
4. 공개 URL = `https://raw.githubusercontent.com/bighitcho/blog-assets/master/<out>`. 배경 원본은 `<out>_bg.jpg` 로 같이 커밋된다.

템플릿: 0 = 좌측 텍스트·우측 사진 살림 / 1 = 하단 텍스트·상단 사진 / 2 = 중앙 텍스트·가장자리 사진.
**dim 0.25~0.32 (상한 0.40), blur 3 (상한 4).** 2026-09-04 이전 기본값(dim 0.55, blur 5)과 스크림 그라디언트가
겹쳐 하단에서 원본 사진이 7%만 남아 "사진이 아예 안 보인다"는 지적을 받았다. `cloud_thumb.cjs` 가
기본값·상한을 강제하므로 요청 JSON 에 큰 값을 넣어도 잘린다. 사진이 주인공, 글자는 그 위다.
(GitHub 커넥터가 있는 세션에서는 `actions_run_trigger`(workflow_id thumb.yml, inputs out/bg_url/spec)로 즉시 디스패치해도 된다.)

### 11-6. 본문 이미지 — 소주제마다 사진 (2026-09-03 사장님 지시)

대표 썸네일만으로는 부족하다. **글 하나에 본문 사진 2~3장**을 소주제(H2)에 맞춰 넣는다.

1. 어떤 H2 아래에 어떤 장면이 어울리는지 먼저 정한다(계산 섹션 → 앱 결제 장면, 환급 섹션 → 시장 장보기, 동선 섹션 → 그 길의 풍경 등).
2. Higgsfield `generate_image_batch`(soul_location, 16:9)로 한 번에 생성한다. 프롬프트 끝에 "editorial photograph, no text, no logos, no signs".
3. `thumb_requests/<blog>_<slug>_p1.json` 처럼 요청 파일을 쓰되 **`"mode":"photo"`** 를 넣는다:
   `{"mode":"photo","out":"batch_YYYYMMDD/<blog>_<slug>_p1.jpg","bg_url":"<result_url>"}`
   (합성 없이 내려받아 폭 1200px JPEG 로 저장된다.) 썸네일 요청과 같이 push 하면 한 번에 렌더된다.
   사진에 깨진 표지판·인물 조각이 있으면 `"crop":[left,top,right,bottom]`(0~1 비율)을 넣어 잘라낸다. 예) 상단 20%·우측 20% 제거 = `[0,0.2,0.8,1.0]`. 같은 out 으로 다시 요청하면 덮어쓴다(요청 파일명만 다르게).
4. 본문에는 해당 H2 바로 아래 첫 문단 뒤에 넣는다:
   `<figure style="margin:18px 0;"><img src="<raw URL>" alt="<장면을 문장으로>" style="width:100%;height:auto;border-radius:8px;" loading="lazy" /><figcaption style="font-size:13px;color:#777;margin-top:6px;">▲ <한 줄 설명></figcaption></figure>`
   alt 는 키워드 나열이 아니라 장면 설명. 캡션은 본문 내용과 이어지는 한 줄.
5. 이미지 없이 글을 올리지 않는다. 8분 넘게 렌더가 안 오면 썸네일만으로 초안을 만들고 보고에 "본문 이미지 미삽입"을 적는다.

## 12. 발행량·자동발행 규칙 (2026-09-05 최종 확정 — 승인 대기 폐지, 하루 3편)

**모든 블로그(블로그스팟 12개 + 워드프레스 3개) 공통: 매일 블로그당 3편(즉시발행 1 + 예약발행 2), 06:00 KST 기준.**
블로그스팟은 PC(`daily_run.py` → `publish_wave.py`)가, 워드프레스 3개(mydooba·sunyhill·eblueribbon)는 클라우드 루틴이 맡는다.
어느 쪽이든 §4·§5의 승인 절차는 폐지 — 아래 12-3 셀프 검수를 통과한 글은 사람 확인 없이 발행한다. "발행할까요?"라고 묻지 않는다.

> **편수 변경 이력 — 하루 안에 세 번 바뀌었다, 이 순서로 읽는다.**
> ① 2026-08 말: 하루 3~5편(즉시 1 + 같은 날 예약 2~4)으로 돌렸을 때 네트워크 전체가 하루 45~60편씩
> 쏟아져 **jenyjelly·golden628 이 애드센스 저가치 콘텐츠 판정**을 받았다 → "1일 1편"으로 축소(2026-09-02).
> ② 2026-09-05 오전: 사장님께 이 이력을 다시 안내하고 확인한 결과 **"1일 1편 유지"** 선택 → 그대로 실행.
> ③ 2026-09-05 오후: 사장님이 ①의 이력을 **아신 상태에서** 그럼에도 **"사이트당 3편(즉시1+예약2), 매일 06:00"** 으로
> **최종** 확정. 같은 날 두 번 확인된 지시이므로 이게 마지막이고 맞다 — **다시 1일 1편으로 되돌리지 말 것.**
> 대신 §14(SEO·GEO·AEO)·§12-3(셀프 검수)를 엄격히 지켜 "저가치 양산"이 아니라 "원본 가치 있는 3편"이 되게 한다.
> PC 쪽 `daily_config.json` 의 `counts`: 블로그스팟 12개(goldnforest 포함)는 3, 워드프레스 3개는 **0**(클라우드 루틴과 이중발행 방지). start 06:00, gap_min 240.

### 12-1. 하루 목표와 채우기 (워드프레스, 클라우드 루틴)
- 기준: 오늘(KST) 날짜의 글 수 = publish + future 합산, PC가 올린 글 포함. **사이트당 3편(즉시1+예약2)이 될 때까지** 쓴다(중복 생산 금지).
- 루틴은 **하루 한 번 06:00 KST**(cron `0 21 * * *` UTC)에 돌아 그 실행 안에서 9편(사이트당 3편)을 끝낸다. 시작할 때 `posts.list`로 세고 부족분만 채운다.
- 순서: 세 사이트의 즉시발행 1편씩을 먼저 끝낸다. 그다음 사이트마다 예약 2편(12-2 시각 규칙)을 채운다. 이미지는 배치로 묶어 요청한다.
- 끝나기 전에 `posts.list`로 사이트당 3/3을 다시 세고, 모자라면 그 자리에서 채운다(별도 안전망 루틴 없음).

### 12-2. 예약 시각 규칙
- 사이트 시간대 = KST (2026-09-03 확인: posts 의 date 가 KST 로 찍힘). `posts.create`(status "future", date "YYYY-MM-DDTHH:MM:SS").
- 2번째 글 12:00~15:30, 3번째 글 17:30~21:30 사이. **분은 랜덤**(정각·30분 금지 — 자동화 티가 난다). 같은 사이트 글끼리 90분 이상 간격.
- 구간이 이미 지났으면 지금+40분 이후 랜덤. **과거 시각으로 예약하면 그 자리에서 즉시 발행돼 버린다**(PC 2026-08-15 실제 사고).
- 시즌 글(예: 추석 통행료)은 검색 급등 시작 2~3일 전 아침에 예약해 색인 시간을 확보한다. (2026-09-03 결정: 통행료 글 2건은 9/10 아침 예약.)

### 12-3. 셀프 검수 체크리스트 (발행·예약 전, 글마다)
1. 핵심 사실 3개를 공식 출처(korea.kr·부처·공단·공식 공지)에서 WebSearch/WebFetch 로 다시 확인. 날짜·금액·기간이 **제목·슬러그·본문·표·FAQ·excerpt** 에서 전부 일치하는가. (2026-09-03 실제 사례: 본문은 나흘인데 슬러그가 three-days 로 남아 있었다.)
   - **연휴 길이는 달력에서 직접 센다.** 설·추석 연휴는 **일요일·다른 공휴일과 겹칠 때만** 대체공휴일이 붙고 **토요일과 겹치면 없다**(3·1절·광복절·개천절·한글날·부처님오신날·성탄절은 토요일도 대체). 2026 추석 = 9/24(목)~26(토) → 27(일)까지 나흘, **28일(월)은 평일**. 2026-09-04 실제 사고: "닷새 연휴·28일 대체공휴일" 전제로 쓴 글 3편(mydooba 672, eblueribbon 447·449)을 고쳤다.
   - 연휴 한정 혜택과 상시 제도를 구분한다(예: KTX 10% 인하는 9/1부터 상시).
2. 벤치마크: 같은 키워드 네이버 블로그 상위 5개(`NaverSearch-search_blog`)와 비교해 우리 글에만 있는 요소 1개 이상(직접 계산·비교표·날짜 대입·속설 반박). 없으면 추가한 뒤 올린다.
3. 이미지: 썸네일 v3(사진 배경) + 본문 사진 2장 이상, 모든 raw URL 이 `curl -sI` 200, alt 는 장면 문장.
   - **`featured_media` 를 반드시 세팅한다.** 목록·카드 그리드는 본문의 `<img>` 가 아니라 `featured_media` 를 쓴다.
     본문 맨 위에 이미지가 있어도 `featured_media` 가 0이면 사이트에서는 "사진 없는 글"로 보인다
     (2026-09-04 사고: 워드프레스 3개 17편 전부 이 상태였다).
   - **배경 사진이 보이는지** 눈으로 확인한다(§11-5). 새까맣게 눌렸으면 다시 만든다.
   - `media.create` 로 올렸으면 응답의 `media_details.filesize` 가 원본 파일 바이트 수와 **정확히 일치**하는지 본다.
     크기가 같아도 내용이 깨질 수 있으므로(2026-09-04 sunyhill 907: 8784B 로 크기는 맞는데 화면은 초록 노이즈),
     **base64 를 손으로 옮겨 적었으면 업로드 전에 원본 파일과 `diff` 로 대조**한다. 다르면 그 구간만
     원본에서 잘라 붙여 다시 맞춘 뒤 올린다. 어긋난 미디어는 `media.delete` 로 지우고 새로 올린다.
   - 본문 사진이 GitHub raw URL 이면, **글을 올리기 전에 이미지가 먼저 push 돼 있어야 한다.** 순서가 반대면
     Jetpack Photon 이 404 를 캐시해 파일을 나중에 올려도 계속 깨져 보인다(고칠 때는 URL 뒤에 `&cb=날짜` 를 붙여 캐시를 깬다).
4. 링크: 내부링크 3개 이상이 실제 URL(posts.list 로 확인), 외부는 공식만 ≤2.
5. 한글 깨짐 없음(§8), AI 티 표현 없음(§3), 본문 해시태그·정형 소제목 없음.
6. 통과 못 하면 고친다. 못 고치면 그 글은 버리고 다른 키워드로 다시 쓴다.

### 12-4. 블로그스팟(PC) 쪽 적용 — 크롬(클라우드)에서 직접 제어
- PC 작업 폴더(`G:\내 드라이브\@알파남@\블로그수익화\블로거-자동포스팅\`)는 Google Drive 데스크톱 미러다. 드라이브 폴더 `1DztWZwR0iLQ-4_vzY65sR9If07hzx5EN`(하위 `briefs` = `1nZxQNGPTZULGD_WIt1ZZ4l-1j3U1Ij-m`)에 파일을 놓으면 PC에 그대로 내려간다(logs·token.json 이 실시간 갱신되는 것으로 2026-09-03 확인).
- 따라서 `daily_config.json`(편수 counts·start·gap_min·모델)과 `briefs/*.md`(글 규칙)는 클라우드에서 교체하면 된다. Drive MCP 는 내용 수정을 지원하지 않으므로 **trash_file → create_file(같은 이름, 같은 parentId, textContent)** 순서로 교체하고, 곧바로 download_file_content 로 다시 받아 내용을 대조한다. PC 실행 시각(06:00 KST 예약 작업, PC가 꺼져 있으면 켜질 때 실행) 직전에는 교체하지 않는다.
- `publish_wave.py` 는 블로그당 첫 편 즉시 + 나머지 gap 간격 예약(분 ±14 랜덤)을 이미 지원한다. **2026-09-05 최종 확정 설정: 블로그스팟 12개 counts 전부 3**(greencap 0 유지, 워드프레스 3개는 0), start 06:00, gap_min 240. 2026-08 '5편/일' 과다발행이 애드센스 저가치 판정(jenyjelly·golden628)의 원인으로 기록돼 있으나, 2026-09-05 사장님이 이 이력을 아신 채로 "사이트당 3편, 06:00"을 최종 확정했다(§12 머리말 ③). §14 SEO/AEO/GEO 기준과 12-3 셀프 검수로 저가치 재발을 막는 데 집중한다.
- 컨테이너에서 googleapis.com 이 열리는 것도 확인했다(Blogger API 직접 호출 가능, token.json 은 드라이브 PC 폴더에 있음). 블로그스팟 원고 생성까지 클라우드로 옮기는 건 사장님 결정 사항(하루 36편 → 세션 사용량 급증).
- `daily_run.py` 는 `briefs/_공통.md` 와 `briefs/{blog}.md` 만 읽는다. 추가 규칙 파일(`_공통_추가규칙_*.md`)은 PC가 읽지 않으므로, 규칙을 바꾸려면 `_공통.md` 자체를 교체해야 한다(§8 한글 검수 필수).

## 13. 지침 관리 원칙 (2026-09-04 사장님 지시)

사장님 원문: *"항상 지침사항이 변경되면 최근 변경내용으로 적용시키고 지침사항을 저장해둘 것."*
*"내가 항상 지적하는 사항은 행동 수정하라는 거야."*

1. **최신 지시가 무조건 이긴다.** 이 런북·브리프·설정 파일에 적힌 옛 규칙과 사장님의 최근 지시가
   부딪히면, 묻지 말고 **최근 지시** 를 따른다. 그리고 그 자리에서 이 파일을 고쳐 옛 규칙을 덮는다.
   ("예전에 이렇게 하라고 하셔서" 는 사유가 되지 않는다.)
   - 예외는 하나: 최근 지시를 그대로 따르면 **애드센스 정책 위반·수익 정지 같은 되돌리기 어려운 손해**가
     확실할 때. 그때만 근거를 들어 한 번 확인받고, 답을 받은 즉시 그대로 반영한다(§12 편수 건이 그 사례).
2. **지적은 1회성 수정이 아니라 행동 수정이다.** 같은 지적을 두 번 받지 않도록, 고친 내용을 반드시
   이 런북(또는 해당 코드·브리프)에 남긴다. 지적받은 글만 고치고 끝내지 않는다.
3. **바꾼 즉시 저장한다.** 새 지시를 받으면 → 코드/설정 수정 → 이 런북에 날짜·원문 요지·바꾼 값을 적고
   → `git push`. 세션이 끊겨도 다음 세션이 같은 규칙으로 돌게 만드는 게 목적이다.

### 13-1. 사이트별 말투

| 사이트 | 언어 | 말투 |
|---|---|---|
| mydooba | 한국어 | 생활경제 브리핑. 평서체(-다), 숫자·날짜 앞세우고 군더더기 없음. |
| sunyhill | 한국어 | 살림·요리 큐레이션. 평서체(-다) 기본, 겪어본 사람의 설명 톤. 과장·감탄 금지. |
| eblueribbon | 영어 | **한국에 사는/오는 외국인이 읽는 현대 영어 블로그 말투.** (2026-09-04 사장님 지시) |

**eblueribbon 말투 규칙 (2026-09-04 신설)**
- 요즘 영어권 개인 블로거가 쓰는 자연스러운 구어체. 사람이 사람에게 설명하는 문장.
  "you"를 쓰고, 짧은 문장과 긴 문장을 섞고, 필요하면 한 줄짜리 문단도 쓴다.
- 금지: 관공서 번역투, 백과사전 서술, "Furthermore/Moreover/In conclusion" 류의 접속 상투구,
  "Let's dive in", "In today's fast-paced world" 같은 AI 상투 도입부, 느낌표 남발, 이모지.
- 한국어 원문 직역 금지. 한국 고유명사는 영어권 독자가 모른다고 보고 한 줄로 풀어 준다
  (예: "songpyeon, the half-moon rice cake Koreans make for Chuseok").
- 원화 금액은 처음 나올 때 대략의 달러 환산을 괄호로 붙인다. 날짜는 24 September 형식.
- 실용 정보가 먼저다. 감상·문화 예찬으로 시작하지 않는다.

## 14. SEO·GEO·AEO 최적화 — 필수 지침 (2026-09-04 사장님, 최우선)

사장님 원문: *"가장 중요한 건 seo, geo, aeo 최적화 블로그 셋팅이다. 모든 컨텐츠는 이 적용에 맞춰
기획하고 떡상시킬 컨텐츠로 연구해서 주제와 키워드 확장으로 작성한다. 무조건 상위 노출·조회수가
많이 나온 컨텐츠의 주제를 찾아 글을 작성한다. 필수지침."*

이 절은 §11(주제 선정)·§12(발행량)보다 **위**에 있다. 여기 어긋나는 글은 쓰지 않는다.

### 14-0. 셋은 다른 것이다 (한 글에서 동시에 만족시킨다)
| 약자 | 대상 | 우리가 얻는 것 |
|---|---|---|
| **SEO** | 구글·네이버 검색 순위 | 클릭 = 애드센스 노출 |
| **AEO** | 답변 엔진(구글 AI 개요·피처드 스니펫·음성) | 0번째 자리 노출 |
| **GEO** | 생성형 엔진(ChatGPT·Perplexity·Gemini) | AI 답변에 인용·출처로 등장 |

### 14-1. 주제는 "이미 터진 것"에서 고른다 (발명 금지)
새 주제를 창작하지 않는다. **이미 조회수·상위노출이 검증된 주제**를 찾아 그보다 나은 글을 쓴다.
1. **수요 검증** — `NaverSearch-datalab_search`(timeUnit "date", 21일). 오늘 지수가 후보군 최고치의
   50% 이상이거나 3일 연속 상승 중인 것만 통과(§11).
2. **상위 글 해부** — `NaverSearch-search_blog`(display 10) + WebSearch 로 구글 1페이지를 본다.
   상위 글마다 ① 제목 패턴 ② H2 구성 ③ 인용한 숫자 ④ **답하지 않은 질문** 을 메모한다.
3. **조회수 교차 검증** — `YouTubeData-search_videos` 로 같은 키워드 영상의 조회수·댓글 질문을 본다.
   조회수 상위 영상이 다루는 각도 = 사람들이 실제로 궁금해하는 것.
4. **우리 각도** — 2·3에서 아무도 답하지 않은 것 1개 이상(직접 계산·비교표·날짜 대입·속설 반박).
5. **키워드 확장** — 메인 키워드 1개 + 롱테일 3~5개를 H2/H3/FAQ 에 배치한다.
   확장 소스: 네이버 연관검색어·자동완성, 구글 "사람들이 함께 찾는 질문", 상위 글의 소제목.
   유형: 질문형("~하면 얼마") / 연도형("2026") / 지역형 / 비교형("A vs B") / 조건형("~일 때").
6. **토픽 클러스터** — 한 주제를 하루로 끝내지 않는다. 하위 질문 2~3개를 다음 날들 글로 잇고
   서로 내부링크한다(허브-스포크). 같은 주제군이 쌓여야 그 주제의 권위가 생긴다.

### 14-2. AEO — 답변으로 뽑히게 쓰는 구조 (글마다 필수)
- **직답 먼저**: 제목 바로 다음 첫 문단에 검색 질문의 답을 **한 문장(40~60자)** 으로 못 박는다.
  근거·배경은 그다음. (예: "2026년 추석 연휴는 9월 24일부터 27일까지 나흘이다.")
- **H2·H3 는 검색창에 치는 질문 문장** 그대로. ("송편 반죽이 갈라지는 이유는?")
- 각 H2 아래 **첫 문장이 그 소제목의 답**. 스니펫은 그 한 문장을 가져간다.
- 숫자·금액·기간·조건은 **표**로. 표 첫 열 = 항목, 둘째 열 = 값. 리스트보다 표가 잘 뽑힌다.
- FAQ 3개 이상(§3). 질문은 실제 검색어 형태로.
- 한 문단 3~4줄. 답변 엔진은 긴 문단을 통째로 버린다.

### 14-3. GEO — AI가 인용하게 쓰는 법
- **자립형 문장**: 앞 문장을 안 읽어도 뜻이 통하게 주어·수치·기간을 한 문장에 다 넣는다.
  ("정부는 9월 3일부터 사과·배·소고기를 최대 40% 할인한다" ○ / "이번엔 40%다" ✗)
- **모든 수치에 출처와 확인일**. 기관 정식 명칭 + 발표일. 출처 없는 수치는 AI가 인용하지 않는다.
- **원본 데이터 1개 이상** — 직접 만든 비교표, 직접 한 계산. 남의 글에 없는 것이 인용 이유가 된다.
- **개체(entity) 명확화**: 제도·기관·상품은 처음 등장할 때 정식 명칭 풀네임 → 이후 약칭.
- **E-E-A-T 신호**: 저자·업데이트일·About·문의 페이지가 있는 사이트가 인용된다. 세 사이트 모두 유지.
- 영어 사이트(eblueribbon)는 §13-1 말투를 지키되, 위 규칙은 동일하게 적용한다.

### 14-4. 사이트 셋팅 (기술 SEO) — 상태와 기준
2026-09-04 실측 → **같은 날 조치 완료**:

| 항목 | mydooba | sunyhill | eblueribbon |
|---|---|---|---|
| SEO 플러그인 | Rank Math 1.0.277.2 활성 | **Rank Math 설치·활성(09-04)** | **Rank Math 설치·활성(09-04)** |
| Site Kit(GSC 연동) | 활성 | 활성 1.186.0 | 활성 1.186.0 |
| ads.txt / 동의배너 | 활성 | 활성 | 활성 |

**Rank Math 최초 1회 설정(wp-admin 에서 사람이 해야 하는 부분)** — 플러그인 설치·활성화는 MCP 로 되지만
설정 마법사는 안 된다. sunyhill·eblueribbon 각각 `wp-admin → Rank Math SEO → Setup Wizard`:
1. 모드 **Advanced**, 사이트 유형 **Blog**(eblueribbon 은 언어 English)
2. Google Search Console 연결(Site Kit 이 이미 붙어 있으면 계정 선택만)
3. Sitemap **ON** (posts·pages·categories 포함, tags 는 OFF — 얇은 페이지 색인 방지)
4. Schema 기본값: 글=**Article**, 사이트=**Organization**(이름·로고), BreadcrumbList **ON**
5. 제목 템플릿 `%title% %sep% %sitename%`, 메타설명은 글마다 직접 작성(자동 생성 OFF)
6. noindex: 태그 아카이브·검색결과·작성자 아카이브 ON / 글·카테고리는 절대 OFF 금지

- **JSON-LD 주의**: `seo_enrich.py` 는 Article + FAQPage 를 본문에 넣지만, 워드프레스는 본문 안의
  `<script>` 를 지워 버릴 수 있다. 2026-09-04 발행분에는 구조화 데이터가 실제로 없었다.
  **스키마는 본문이 아니라 SEO 플러그인(템플릿 레벨)에서 넣는다.**
- 세 사이트 공통 기준: 글 스키마 = Article, FAQ 있으면 FAQPage, 사이트 = Organization,
  빵부스러기(BreadcrumbList) 켜기, XML 사이트맵 제출, OG/트위터 카드, 제목 템플릿
  `%title% %sep% %sitename%`, 메타설명은 글마다 직접 작성(80~160자, §2 excerpt 와 동일 문장 금지).
- 카테고리는 3~6개로 유지하고 **미분류 금지**(§12-3). 카테고리 = 토픽 클러스터의 허브다.

### 14-5. 발행 전 SEO·AEO·GEO 체크 (§12-3 에 더해서)
1. 첫 문단에 40~60자 직답이 있는가
2. H2 가 질문 문장이고, 각 H2 첫 문장이 답인가
3. 표 1개 이상 + FAQ 3개 이상
4. 모든 수치에 출처·확인일, 자립형 문장인가
5. 메인 키워드가 제목·슬러그·첫 문단·H2 1곳에 있고, 롱테일 3개 이상이 본문에 자연스럽게 있는가
6. 같은 클러스터의 우리 글 3개 이상으로 내부링크했는가
7. featured_media·카테고리 지정, 메타설명 작성 완료
